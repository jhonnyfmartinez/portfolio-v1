{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next",
      "ignore": "message:dependabot"
    }
  ],
  "routes": [],
  "env": {
    "VERCEL_GIT_COMMIT_REF": "${VERCEL_GIT_COMMIT_MESSAGE.includes('dependabot') ? '' : VERCEL_GIT_COMMIT_REF}"
  }
}
